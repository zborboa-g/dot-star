# Use case-insensitive file name tab autocomplete.
set completion-ignore-case on

# Tab once for autocompletion.
set show-all-if-ambiguous on
set show-all-if-unmodified on

# Append a character denoting a file's type to the filename when listing
# possible completions. Like `ls -F'.
set visible-stats on

# Use vi key bindings with readline compatible applications.
set editing-mode vi

# Disabled as using `set keymap vi' causes history-search-backward in ipython to no longer work.
# https://stackoverflow.com/questions/16350245/ipythons-history-search-backward-not-working-as-desired
# https://stackoverflow.com/questions/10394302/how-do-i-use-vi-keys-in-ipython-under-nix
# set keymap vi
